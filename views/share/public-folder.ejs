<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= folder.name %> - Shared Folder</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap"
      rel="stylesheet"
    />
   
    <link rel="stylesheet" href="/styles.css">
    
    <style>
        
        .public-container { max-width: 900px; margin: 40px auto; padding: 20px; }
        .header { text-align: center; margin-bottom: 40px; }
        .grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(150px, 1fr)); gap: 20px; }
        .card { border: 1px solid #ddd; padding: 15px; border-radius: 8px; text-align: center; background: white; }
        .btn-download { display: block; margin-top: 10px; background: #007bff; color: white; text-decoration: none; padding: 5px; border-radius: 4px; }
        .btn-download:hover { background: #0056b3; }
         .meta-info {
            font-weight: 500;
        }
    </style>
</head>
<body>

    <div class="public-container">
        
        <div class="header">
            <h1>📂 <%= folder.name %></h1>
            <p class="meta-info">Shared by <strong><%= owner %></strong></p>
            <small>This link is temporary and read-only.</small>
        </div>

        <div class="grid">
            
            <% files.forEach(file => { %>
                
                <% 
                   const safeName = file.originalName.split('.')[0];
                   // This logic forces the file to download instead of opening
                   const downloadUrl = file.url.replace('/upload/', `/upload/fl_attachment:${safeName}/`);
                %>

                <div class="card">
                    <div style="font-size: 3rem; margin-bottom: 10px;">
                        <% if (file.url.endsWith('.pdf')) { %> 📄 <% } else { %> 🖼️ <% } %>
                    </div>

                    <div style="font-weight: bold; overflow: hidden; text-overflow: ellipsis; white-space: nowrap;">
                        <%= file.originalName %>
                    </div>
                    
                    <div style="color: #666; font-size: 0.8rem; margin-bottom: 10px;">
                        <%= (file.size / 1024).toFixed(1) %> KB
                    </div>

                    <a href="<%= downloadUrl %>" class="btn-download">Download</a>
                </div>

            <% }) %>

        </div>

        <% if (files.length === 0) { %>
            <p style="text-align: center;">This folder is empty.</p>
        <% } %>

    </div>

</body>
</html>